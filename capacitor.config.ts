import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.djenkins.capacitorapp',
  appName: 'capacitor-app',
  webDir: 'dist/capacitor-app',
  bundledWebRuntime: false
};

export default config;
